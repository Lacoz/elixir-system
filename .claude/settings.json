{
  "hooks": {
    "PreCompact": [
      {
        "hooks": [
          {
            "command": "command -v tk >/dev/null && tk help | head -n 24 || command -v ticket >/dev/null && ticket help | head -n 24 || true",
            "type": "command"
          }
        ],
        "matcher": ""
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "command": "command -v tk >/dev/null && tk help | head -n 24 || command -v ticket >/dev/null && ticket help | head -n 24 || true",
            "type": "command"
          }
        ],
        "matcher": ""
      }
    ]
  }
}
